# flutter_comics

Shared `.comics`/`.puzzle` data model, keyframe interpolation, and `.lottie` import/export.
Portable — no native-core/FFI dependency. Consumed by `apps/comics-editor` and
`libs/comics_viewer/flutter_comics_viewer`.

See `flows/sdd-flutter-comics/` for the design history.
