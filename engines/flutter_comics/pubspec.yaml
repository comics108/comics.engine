name: flutter_comics
description: "Shared .comics/.puzzle data model, keyframe interpolation, and .lottie import/export -- portable, no native-core dependency."
version: 0.1.1
homepage: https://github.com/libsFlutter/flutter_comics
repository: https://github.com/libsFlutter/flutter_comics
issue_tracker: https://github.com/libsFlutter/flutter_comics/issues

topics:
  - comics
  - plugin
  - native
  - web

environment:
  sdk: ^3.12.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.0.9
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
